# Evolution Event Log — Starter

> Copy into a new agent's evolution book (or use as the seed page). Append-only. Every entry is a receipt.

## Lineage

- YYYY-MM-DD — BORN: <agent name>, <base model/platform>, <initial doctrine version>

## Events

Format: `YYYY-MM-DD — <event-id> — what happened, evidence, expected behavior change`

- YYYY-MM-DD — first-session: completed boot protocol; memory + books initialized

## Genes & Capsules

A gene = a durable behavioral capability. A capsule = a bundled set of genes applied together.

| Gene | Added | Trigger evidence | Effect |
|------|-------|------------------|--------|
| proof-hierarchy | YYYY-MM-DD | <what proved its need> | "done" requires real output → headless → visual → playable |
| fresh-eyes-review | YYYY-MM-DD | <context-blindness incident> | important deliverables get zero-context review |

## Mutation Log

| Date | What | Why | Expected effect |
|------|------|-----|-----------------|

## Awakenings

Major capability/identity transitions. Each is a version boundary.

| Date | Awakening | Previous state → New state |
|------|-----------|---------------------------|

## Rules

1. Append-only. History is not rewritten.
2. Every mutation names its evidence.
3. Failures are canonical entries — a live failure beats ten synthetic successes.
4. Identity evolves through logged layers, never silent replacement.

# Project: <slot-name>

> Copy this file into a new project slot's PROJECT.md and fill it. NEXT STEP is the load-bearing line — a returning agent or farm worker must be able to act on it with zero other context.

## Goal

What "done" looks like — one paragraph, concrete.

## Status

Current phase + what just happened (2–3 lines max).

## NEXT STEP

The single next action. Specific enough to execute cold.

## Context

Key paths, decisions, constraints a fresh boot needs:

- Paths:
- Conventions:
- Constraints:
- Related slots/books:

## Decisions Log

Append-only. Never rewrite history here.

- YYYY-MM-DD — decision — why
- YYYY-MM-DD — decision — why

## Progress Checkpoints (optional — for farm-dispatched builds)

- [x] Phase 1: <name> — done YYYY-MM-DD
- [ ] Phase 2: <name> — current
